<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.membership.mybatis.mapper.OrderMapper">
    <resultMap id="orderResultMap" type="org.example.membership.mybatis.entity.Order">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="orderAmount" column="order_amount"/>
        <result property="status" column="status"/>
        <result property="orderedAt" column="ordered_at"/>
    </resultMap>

    <select id="findAll" resultMap="orderResultMap">
        SELECT * FROM orders
    </select>

    <select id="findByUserId" resultMap="orderResultMap">
        SELECT * FROM orders WHERE user_id = #{userId}
    </select>

    <insert id="insert" parameterType="org.example.membership.mybatis.entity.Order" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders (
            user_id,
            order_amount,
            status,
            ordered_at
        ) VALUES (
            #{userId},
            #{orderAmount},
            #{status},
            #{orderedAt}
        )
    </insert>
</mapper> 